import {
  Dialog,
  DialogActions,
  DialogContent,
  DialogTitle,
} from "@mui/material";

export default function GenericModal({
  open,
  closeModal,
}: {
  open: boolean;
  closeModal: () => void;
}) {
  return <></>;
}
