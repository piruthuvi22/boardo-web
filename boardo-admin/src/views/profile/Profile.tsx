import React from "react";
import { useTheme } from "@mui/material";

const Profile = () => {
  const theme = useTheme();
  return (
    <>
      <h1>Profile</h1>
    </>
  );
};

export default Profile;
